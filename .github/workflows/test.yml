name: "Test"
on: [push, pull_request]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    steps:
      - uses: jfrog/setup-jfrog-cli@v2
        env:
          JF_ENV_1: ${{ secrets.JF_SECRET_ENV_1 }}
      - run: |
          jf npm i
          jf bp
